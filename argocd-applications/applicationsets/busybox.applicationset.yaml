apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: busybox-applicationset
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - list:
      elements:
      - index: "1"
      - index: "2"
  template:
    metadata:
      name: 'busybox-{{.index}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/david-bour/local-gitops.git
        path: argocd-applications/applications
        targetRevision: HEAD
      destination:
        server: "https://kubernetes.default.svc"
        namespace: busybox
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true
